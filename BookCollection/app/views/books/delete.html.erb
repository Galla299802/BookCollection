<h1>Show Details</h1></br>
<%= link_to('Back to Home page', {:action => 'index'})%>

<div class="books delete">
    <h2>Delete Book</h2>

    <%= form_for(@book, :method => 'delete') do |f|%>

        <p>Are you sure you want to permanently delete this book?</p>

        <p class="rederence-name"><%= f.object.title%></p>

        <div class="form buttons">
            <%= f.submit("Delete Subject")%>
        <div>

    <%end%>
</div>